/* 
#include <unistd.h>
#include <stdio.h>

int main() {
    int x = 10;
    int* ptr = &x;
    
    printf("Address of x (hexadecimal): %p\n", ptr);
    printf("Address of x (decimal): %lx\n", (unsigned long)ptr);
    return 0;
} */